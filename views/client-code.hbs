
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Invoice Details</title>
    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        body {
            background-color: #f8f9fa;
            min-height: 100vh;
            display: flex;
            align-items: center;
            padding: 20px;
            margin: 0;
            overflow: hidden; /* Prevent scrolling */
        }
        
        .card {
            border: none;
            border-radius: 10px;
            box-shadow: 0px 0px 10px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 400px;
            margin: 0 auto; /* Center the card */
        }
        
        .card-title {
            font-size: 1.5rem;
            margin-bottom: 0.5rem;
        }
        
        .card-text {
            color: gray;
            margin-bottom: 1rem; /* Reduced margin */
        }
        
        .btn-primary {
            background-color: black;
            border: none;
            padding: 10px;
            transition: 0.3s;
        }
        
        .btn-primary:hover {
            background-color: #333;
        }
        
        .form-label {
            font-weight: bold;
        }
        
        .form-control {
            margin-bottom: 0.75rem; /* Reduced margin */
            padding: 8px;
        }
        
        /* Adjust spacing for smaller screens */
        @media (max-height: 700px) {
            .card {
                padding: 15px;
            }
            .card-title {
                font-size: 1.3rem;
            }
            .card-text {
                font-size: 0.85rem;
                margin-bottom: 0.75rem;
            }
        }
    </style>

    <div class="container-fluid"> <!-- Changed to fluid container -->
        <div class="row justify-content-center">
            <div class="col-12 col-sm-10 col-md-8 col-lg-6 col-xl-4">
                <div class="card p-3"> <!-- Reduced padding -->
                    <div class="card-body text-center p-2"> <!-- Reduced padding -->
                        <h2 class="card-title mb-2">Enter Details</h2>
                        <p class="card-text mb-3">Enter the client code you received to view your invoice details.</p>
                        
                        <form id="invoiceForm" action="/clientLogin" method="post">
                            <div class="mb-2 text-start"> <!-- Reduced margin -->
                                <label for="paymentCode" class="form-label">Client Code</label>
                                <input type="text" name="clientCode" class="form-control" id="paymentCode" placeholder="Enter your client code" required>
                            </div>
                            
                            <div class="mb-2 text-start"> <!-- Reduced margin -->
                                <label for="name" class="form-label">Name</label>
                                <input type="text" name="client" class="form-control" id="name" placeholder="Enter your name" required>
                            </div>
                            
                            <button type="submit" class="btn btn-primary w-100 mt-2">View Invoice →</button>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <!-- Bootstrap JS Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
    
   